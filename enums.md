# Enums

Enums represent a fixed set of named values.

```art
enum State {
    Idle,
    Running,
    Dead
}
```

Enum values can be used in comparisons, conditions, and other expressions where the corresponding value is expected.