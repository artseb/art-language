# Modules

ART supports importing code from other files.

Imports allow larger projects to be split into separate modules instead of placing everything in one script.

The import system also supports assigning an imported module to a name with `as`.